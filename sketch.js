function setup() {
    createCanvas(800, 800);
    background('white');
  }
  
  function draw(){ stroke('pink');
    fill('red'); 
   
  
   // console.log(mouseIsPressed)
  if(mouseIsPressed) {rect(mouseX, mouseY, 25, 35);}
                 }
  // console.log(righ)